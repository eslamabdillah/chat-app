<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="messageDataBinding"
            type="com.example.chatapp.firstore.model.Message" />

    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{messageDataBinding.senderName}"
            android:textSize="24sp"
            tools:text="eslam ahmed" />

        <TextView
            android:id="@+id/message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:background="@drawable/recevice_message_background"
            android:padding="12dp"
            android:text="@{messageDataBinding.content}"
            android:textColor="@color/white"
            android:textSize="24sp"
            tools:text="hello i am eslam"

            />

        <TextView
            android:id="@+id/date_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_marginStart="12dp"
            android:text="@{messageDataBinding.formatTime()}"
            android:textColor="@color/black"
            tools:text="12:00 pm" />


    </LinearLayout>
</layout>